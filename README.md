# HTML Editor

Updated behavior:
- Content, images, links, colors, fonts, font style and font weight are shared between Desktop Edit and Mobile Edit.
- Only layout-specific changes remain mode-specific: font size, line height, width, height, spacing, padding, margin, position and alignment.
- This avoids doing the same work twice across desktop and mobile.
